% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ons_get.R
\name{ons_get}
\alias{ons_get}
\title{Download any ONS time series by CDID code}
\source{
\url{https://www.ons.gov.uk}
}
\usage{
ons_get(cdid, from = NULL, to = NULL, cache = TRUE)
}
\arguments{
\item{cdid}{Character vector. One or more ONS CDID codes (e.g. \code{"IHYQ"}
for quarterly GDP growth). Maximum 50 codes per call.}

\item{from}{Date or character (YYYY-MM-DD). Start date filter. Defaults to
\code{NULL} (all available data).}

\item{to}{Date or character (YYYY-MM-DD). End date filter. Defaults to
\code{NULL} (up to the latest observation).}

\item{cache}{Logical. Use cached data if available (default \code{TRUE}).}
}
\value{
A data frame with columns:
\describe{
\item{date}{Date. Observation date (first day of the period).}
\item{cdid}{Character. The CDID code.}
\item{value}{Numeric. Observation value.}
}
}
\description{
Fetches a time series from the ONS website using its CDID (four-character
series identifier). The function uses the ONS Search API to discover the
download path, then retrieves the CSV.
}
\details{
CDID codes can be found by browsing the ONS time series explorer at
\url{https://www.ons.gov.uk/timeseriestool} or by using \code{\link[=ons_search]{ons_search()}}.
}
\examples{
\donttest{
op <- options(ons.cache_dir = tempdir())
ons_get("IHYQ", from = "2020-01-01")
options(op)
}

}
\seealso{
Other data access: 
\code{\link{clear_cache}()},
\code{\link{ons_search}()}
}
\concept{data access}
